const m = require("../src/m.js");
const assert = require("assert");

describe("", () => {
  it("", () => {
    assert.equal(m.a(null), 0);
    assert.equal(m.a(10), 10);

    assert.equal(m.b(null), 0);
    //assert.equal(m.b(10), 10);
  });

});